# AI 使用记录

## 使用原则

AI 被放在受控工程流程中使用，负责辅助理解、整理、生成候选方案和审查风险，不直接决定上线、回滚或破坏性变更。所有 AI 输出都必须经过基准测试、结构契约测试或人工审阅。

## 关键上下文

- 目标系统：运行多年的 Python CSV 股票移动平均线脚本。
- 新需求：批量处理多只股票、按行业输出、处理停牌缺失数据。
- 关键约束：下游报表依赖旧输出格式，默认不能改文件名、字段顺序、日期格式、数值精度和空值表示。
- 风险背景：源码可获得，但可靠文档不足，现有测试不足。
- 策略选择：先理解和刻画旧行为，再扩展和重构；拒绝一次性大范围重写。

## Prompt 记录

### 1. 理解阶段：入口和数据流梳理

```text
请阅读这个仓库，但不要修改代码。目标是定位遗留 CSV 处理脚本的真实入口、命令行参数、配置来源、调度方式、输入文件路径、输出文件路径和下游消费者。请用表格输出：入口文件、调用方式、读取字段、写出字段、疑似下游报表、证据文件路径、仍需人工确认的问题。不要推断为事实，无法确认的地方标记为待确认。
```

上下文：

- 提供源码目录、历史运行命令、样例 CSV 和输出目录。

约束：

- 不改代码。
- 区分事实、推断和待确认问题。

输出：

- 入口地图。
- 数据流说明。
- 消费者契约候选表。

验证：

- 人工检查入口和调度脚本。
- 用 `rg` 搜索输出文件名、字段名和报表引用。

### 2. 行为刻画阶段：旧行为基线

```text
请基于当前旧脚本和样例输入，整理旧行为特征测试。重点覆盖字段顺序、日期排序、移动平均窗口不足时的输出、空值处理、重复行处理、数值精度和异常退出行为。请输出最小测试样例、预期结果、基准文件生成命令和任何不能确定的旧行为。
```

上下文：

- 提供旧脚本、历史输出、样例数据。

约束：

- 不要求修正旧行为。
- 先保护下游已依赖的行为。

输出：

- 基准输出。
- 特征测试清单。
- 未知行为列表。

验证：

- 对同一输入运行旧脚本和重构后脚本。
- 对旧输出做逐字段或文件 hash 对比。

### 3. 测试阶段：边界数据和矩阵生成

```text
请为股票 CSV 移动平均线脚本构造最小测试矩阵，必须覆盖单股票、多股票、跨行业、停牌缺失交易日、重复行、空字段、旧输出兼容和下游报表冒烟。每个用例请给出输入文件、处理规则、预期输出、风险和验证方式。样例数据要足够小，能人工验算移动平均线。
```

上下文：

- 提供业务规则：重复行保留最后一条，空收盘价不参与计算，停牌不伪造价格。

约束：

- 样例不能依赖真实生产数据。
- 预期结果必须能人工复算。

输出：

- 测试矩阵。
- 最小 CSV 样例。
- 旧输出和行业输出预期结果。

验证：

- 人工复算 `ma3`。
- 检查 `TEST_MATRIX.md` 与 `examples/` 是否一致。

### 4. 重构阶段：模块拆分建议

```text
请在不大范围重写的前提下，为旧脚本提出小步重构切片。目标是把 CSV 解析、结构校验、数据清洗、移动平均计算、行业聚合、输出写入和旧格式兼容适配层拆开。请给出模块职责、输入输出、关键伪代码、每个切片的回归验证方法，以及哪些旧输出契约绝不能被修改。
```

上下文：

- 提供入口地图、旧行为基线、消费者契约和新需求。

约束：

- 不做一次性重写。
- 每个切片都必须能单独验证。

输出：

- 模块拆分表。
- 关键伪代码。
- 重构切片计划。

验证：

- 每完成一个切片运行基准回归。
- 检查旧输出契约没有被模块拆分影响。

### 5. Review 阶段：兼容性审查

```text
请以 Review 视角检查这次遗留脚本改造方案，优先找会影响下游报表的风险。请按严重程度列出问题，重点检查旧输出文件名、字段顺序、日期格式、数值精度、空值表示、排序、退出码、日志和性能。每个问题请给出证据、影响、建议验证方法和是否阻断发布。
```

上下文：

- 提供设计文档、测试矩阵、变更 diff 和验证结果。

约束：

- 先列风险，不先总结优点。
- 没有证据时标记为需验证，不把猜测写成结论。

输出：

- 兼容性问题列表。
- 阻断发布项。
- 发布前必须补测项。

验证：

- 对照下游报表契约逐项检查。
- 阻断项清零后才能进入灰度。

## 拒绝大范围重写建议的记录

曾考虑让 AI 直接把旧脚本重写成一个全新的批处理框架，包括新目录结构、新字段模型和新的输出文件格式。该建议被拒绝。

拒绝原因：

- 题目强调下游报表依赖旧输出格式，大范围重写会扩大兼容性风险。
- 旧脚本缺少可靠文档和测试，直接重写很难证明旧行为没有被破坏。
- 数据处理脚本通常存在隐性业务约定，例如排序、空值、精度、异常退出和文件编码，这些约定需要先刻画再迁移。
- 更稳妥的方案是先建立旧行为基线，再通过兼容适配层和功能开关小步扩展。

最终采用：

- 保留旧路径默认行为。
- 新能力通过新增输出和显式开关启用。
- 每个重构切片都绑定基准回归和兼容性审查。

## AI 输出验证记录

| 阶段 | AI 输出 | 验证方式 | 结论 |
| --- | --- | --- | --- |
| 理解 | 入口地图、消费者契约候选 | 人工核对源码、调度配置和报表引用 | 只接受有证据的事实 |
| 行为刻画 | 基准样例和特征测试 | 旧脚本运行结果对比 | 作为重构前门禁 |
| 测试 | 测试矩阵和 CSV 样例 | 人工复算、文件一致性检查 | 样例用于覆盖关键风险 |
| 重构 | 模块边界和伪代码 | 对照旧输出契约和基准回归 | 按小切片执行 |
| Review | 兼容性风险清单 | 阻断项清零后进入灰度 | 保留人工最终判断 |

