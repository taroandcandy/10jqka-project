# 遗留数据处理脚本扩展与重构：执行计划

## 1. 目标定位

本题的核心不是直接重写一个移动平均线脚本，而是设计并落地一套面向遗留数据脚本的 AI 辅助现代化工作流。

更准确地说，本方案采用“工作流编排层 + 可复用技能节点”的结构：

- 工作流负责定义阶段顺序、输入输出、风险门禁和验收标准。
- 技能负责封装单点能力，例如入口识别、调用链梳理、旧行为刻画、测试生成、兼容性审查、灰度方案生成。
- 每个技能都有明确输入、输出格式、上下文要求、约束和验收条件，避免把所有步骤塞进一个不可控的大提示词。

目标是在支持新需求的同时，保护旧输出格式、旧调用方和下游报表：

- 支持批量处理多只股票。
- 支持按行业输出聚合结果。
- 支持处理停牌导致的缺失交易日。
- 处理重复行、空字段等数据质量问题。
- 保持旧单只股票输出格式兼容。
- 建立可验证、可灰度、可回滚的交付流程。

## 2. 计划交付物

### 2.1 README.md

主交付文档，回答题目中的工作流要求。

内容包括：

- 端到端流程：理解 -> 行为刻画 -> 扩展 -> 重构 -> 验证 -> 灰度交付。
- 工作流编排层和技能节点的关系。
- 每一步的输入、动作、输出。
- 每一步的风险点。
- 每一步的验证方法。
- 对下游报表和存量用户的影响控制。

### 2.2 DESIGN.md

设计文档，回答核心模块划分和伪代码要求。

内容包括：

- 旧脚本现状假设。
- 模块拆分方案。
- 解析、清洗、计算、行业聚合、输出的解耦设计。
- 保持旧格式兼容的适配层设计。
- 关键伪代码。

### 2.3 TEST_MATRIX.md

测试设计文档，回答最小 CSV 示例、预期结果、测试矩阵和兼容性验证要求。

内容包括：

- 最小 CSV 输入样例。
- 覆盖停牌、重复行、空字段、跨行业数据。
- 旧输出格式预期行为。
- 新输出格式预期行为。
- 回归测试矩阵。
- 下游报表兼容性验证方法。
- 灰度指标和回滚条件。

### 2.4 AI_USAGE.md

AI 使用记录，回答题目中关于提示词和 AI 介入过程的要求。

内容包括：

- 理解阶段的提示词。
- 行为刻画阶段的提示词。
- 测试生成阶段的提示词。
- 重构审查阶段的提示词。
- 每条提示词需要包含的上下文、约束、验收条件。
- 一次拒绝 AI 大范围重写建议的记录和原因。

### 2.5 SKILLS_SPEC.md

可复用技能设计文档，说明工作流中的单点操作如何沉淀为可复用能力。

内容包括：

- 技能划分原则。
- 每个技能的职责边界。
- 每个技能的输入、输出、上下文要求、约束和验收条件。
- 技能之间如何由工作流编排，而不是相互隐式调用。
- 哪些技能可以复用到其他遗留脚本治理任务。

计划定义的技能：

- `legacy-data-modernization-workflow`：总控编排理解、行为刻画、扩展、重构、验证、灰度交付全流程。
- `subagent-delegation-planner`：判断哪些节点适合委派给 subagent，并定义任务边界、输入材料、输出工件和回收方式。
- `repo-entrypoint-mapper`：定位入口、参数、配置、调度任务和调用链。
- `consumer-contract-mapper`：识别下游报表、文件消费者和输出契约。
- `legacy-behavior-characterizer`：生成基准样例和旧行为基线。
- `dirty-data-case-designer`：设计停牌、重复行、空字段、跨行业等边界样例。
- `feature-extension-planner`：专门设计多股票批处理、行业输出、停牌缺失数据处理和旧输出兼容的扩展方案。
- `refactor-slicer`：把重构拆成低风险小步变更。
- `regression-validation-planner`：专门设计单元测试、基准文件回归、结构契约测试、下游冒烟、新旧差异和性能验证。
- `compatibility-reviewer`：检查字段、格式、路径、数值精度、排序等兼容风险。
- `gray-release-planner`：设计影子运行、双写、灰度指标和回滚条件。
- `ai-usage-recorder`：专门生成 AI_USAGE.md，记录 AI 在理解、重构、测试和审查中的上下文、输出、验证和拒绝大范围重写的原因。

### 2.6 examples/

可选但建议提供的样例目录，用来增强答案可信度。

计划文件：

- `examples/legacy_single_stock.csv`
- `examples/multi_stock_with_industry.csv`
- `examples/expected_legacy_output.csv`
- `examples/expected_industry_output.csv`

### 2.7 SUBAGENT_STRATEGY.md

subagent 委派策略文档，说明哪些节点适合使用 subagent，哪些节点必须由主代理保留决策权。

内容包括：

- subagent 启用原则。
- 强委派节点、可选委派节点、不可委派决策。
- 每类节点的上下文成本和耗时来源。
- subagent 任务输入模板。
- subagent 输出工件验收标准。
- 主代理如何汇总、去重、解决冲突并形成最终结论。

该文档也可以并入 WORKFLOW.md。如果内容较短，优先放在 WORKFLOW.md；如果后续扩展为团队级规范，则单独维护。

## 3. 分阶段执行流程

### 题目六阶段与技能节点对齐

为避免“流程节点隐含在其他节点中”，最终插件必须显式覆盖题目要求的六阶段：

| 题目阶段 | 显式技能节点 | 当前状态 | 需要补强 |
| --- | --- | --- | --- |
| 理解 | `repo-entrypoint-mapper`、`consumer-contract-mapper` | 已实现 | 无 |
| 行为刻画 | `legacy-behavior-characterizer` | 已实现 | 无 |
| 扩展 | `feature-extension-planner`、`dirty-data-case-designer` | 待新增 | 新增扩展设计技能 |
| 重构 | `refactor-slicer` | 已实现 | 与扩展设计衔接 |
| 验证 | `regression-validation-planner`、`compatibility-reviewer` | 待新增 | 新增回归验证技能 |
| 灰度交付 | `gray-release-planner` | 已实现 | 与验证结果衔接 |
| AI 使用记录 | `ai-usage-recorder` | 待新增 | 新增 AI_USAGE 记录技能 |

最终总控链路应调整为：

```text
subagent 委派规划
-> 理解：入口和下游契约
-> 行为刻画：旧行为基线
-> 扩展：新能力设计和脏数据规则
-> 重构：小步切片
-> 验证：回归、契约、下游、性能
-> 兼容性审查
-> 灰度交付
-> AI 使用记录
```

### 阶段 0：工作流和技能边界定义

目标：

- 先搭建“编排层 + 技能节点”的方法论。
- 避免把所有任务混进一个大流程里，导致 AI 输出不可验证、不可复用。

需要回答：

- 哪些步骤属于工作流编排。
- 哪些步骤适合封装为技能。
- 每个技能的输入输出和验收条件是什么。
- 技能的产物如何进入下一阶段。

输出：

- README 中的总体架构说明。
- SKILLS_SPEC.md。

验收：

- 任一技能都能独立运行、独立验收。
- 工作流只是编排技能，不隐藏关键判断。

### 阶段 0.5：subagent 委派策略设计

目标：

- 识别工作流中上下文消耗大、搜索面广、可独立验收的节点。
- 明确哪些节点可以并行交给 subagent，哪些判断必须由主代理保留。
- 为每个可委派节点定义输入包、输出工件、验收标准和停止条件。

适合强委派的节点：

- `repo-entrypoint-mapper`：需要大量仓库搜索和调用链梳理，适合由 subagent 产出入口地图。
- `consumer-contract-mapper`：需要搜索下游文件、字段和报表依赖，适合由 subagent 产出消费者契约表。
- `legacy-behavior-characterizer`：需要运行旧路径、整理基准输出和测试建议，适合由 subagent 产出旧行为基线。
- `compatibility-reviewer`：适合作为独立审查视角，避免主代理自证正确。
- `gray-release-planner`：需要综合运行、监控、回滚和沟通策略，可由 subagent 先形成候选方案。

适合受控委派的节点：

- `dirty-data-case-designer`：可以由 subagent 设计边界样例，但业务规则必须由主代理统一，避免样例之间规则冲突。
- `refactor-slicer`：可以由 subagent 给出切片建议，但最终切片顺序和变更边界由主代理结合代码现状确认。

不建议委派的内容：

- `legacy-data-modernization-workflow` 总控编排本身。
- 是否接受破坏性变更、是否上线、是否回滚等最终决策。
- 涉及真实生产凭据、真实发布操作、删除或覆盖旧输出的动作。

输出：

- PLAN.md 中的 subagent 委派方案。
- 后续实现时新增或更新 `subagent-delegation-planner` 技能。
- 在总控技能中加入 subagent 编排规则。
- 在各节点技能中加入“是否适合委派”的说明。

验收：

- 每个可委派节点都有明确的输入、输出和验收标准。
- subagent 只产出可审阅工件，不直接做高风险修改。
- 主代理保留最终判断、冲突解决和交付口径统一。

### 阶段 1：需求和评分点拆解

目标：

- 把题目要求拆成可交付的文档结构。
- 明确哪些内容必须回答，哪些内容用于加分。

输入：

- 题目原文。

输出：

- README、SKILLS_SPEC、DESIGN、TEST_MATRIX、AI_USAGE 的章节骨架。

验收：

- 题目中的每一条要求都能映射到至少一个交付文件中的明确章节。

### 阶段 2：遗留系统理解工作流设计

目标：

- 说明拿到源码后的第一步如何做。
- 强调先理解和保护旧行为，再扩展功能。

需要覆盖：

- 定位入口脚本。
- 梳理命令行参数、定时任务、配置文件。
- 查找调用方和下游消费者。
- 识别旧输出文件名、字段顺序、编码、日期格式、数值精度。
- 建立基准文件作为旧行为基线。

输出：

- README 中的“源码理解与旧行为基线”章节。
- AI_USAGE 中的理解阶段提示词。

涉及技能：

- `repo-entrypoint-mapper`
- `consumer-contract-mapper`

验收：

- 能清楚说明如何避免“改完功能但破坏下游报表”。

### 阶段 3：特征测试和样例数据设计

目标：

- 自行构造最小 CSV 数据。
- 用特征测试锁住旧行为和边界行为。

需要覆盖：

- 单只股票正常移动平均。
- 多只股票混合输入。
- 跨行业输入。
- 停牌缺失交易日。
- 重复行。
- 空字段。
- 旧输出兼容。

输出：

- TEST_MATRIX.md。
- examples 目录中的 CSV 样例。

涉及技能：

- `legacy-behavior-characterizer`
- `dirty-data-case-designer`

验收：

- 每个测试场景都有输入、处理规则、预期输出和风险说明。

### 阶段 4：模块拆分和扩展设计

目标：

- 在不大范围重写的前提下，把旧脚本逐步拆成可维护模块。
- 用兼容适配层保护旧输出。

候选模块：

- 输入读取模块。
- 结构校验模块。
- 数据清洗模块。
- 交易日对齐模块。
- 移动平均线计算模块。
- 行业聚合模块。
- 输出写入模块。
- 兼容适配模块。

输出：

- DESIGN.md。

涉及技能：

- `feature-extension-planner`
- `refactor-slicer`
- `compatibility-reviewer`

验收：

- 能看出解析、计算、聚合、输出相互解耦。
- 能看出旧路径和新路径如何共存。

### 阶段 5：验证体系设计

目标：

- 给出从单元测试到下游兼容测试的完整验证方案。

需要覆盖：

- 单元测试。
- 基准文件回归测试。
- 结构契约测试。
- 下游报表冒烟测试。
- 新旧结果差异对比。
- 性能和批量处理测试。

输出：

- README 和 TEST_MATRIX 中的验证章节。

涉及技能：

- `regression-validation-planner`
- `compatibility-reviewer`

验收：

- 每类风险都有对应测试或监控手段。

### 阶段 6：灰度交付和回滚设计

目标：

- 说明如何在生产环境低风险上线。

需要覆盖：

- 功能开关。
- 影子运行。
- 双写输出。
- 小股票池灰度。
- 按行业灰度。
- 监控指标。
- 自动或人工回滚条件。

输出：

- README 和 TEST_MATRIX 中的灰度与回滚章节。

涉及技能：

- `gray-release-planner`

验收：

- 能清楚说明出现问题时如何恢复旧路径。

### 阶段 7：AI 使用记录整理

目标：

- 证明 AI 是嵌入工程流程，而不是无约束生成代码。

至少包含 3 条关键提示词，计划提供 5 条。这里的提示词不再只是散落在文档里的例子，而是作为技能调用模板出现：

- 代码入口和数据流理解提示词。
- 旧行为特征测试生成提示词。
- 边界数据样例设计提示词。
- 模块拆分和兼容性设计提示词。
- 重构后审查提示词。

必须包含：

- 提示词背景上下文。
- 明确约束。
- 输出格式要求。
- 验收条件。
- 人工验证方式。
- 拒绝大范围重写建议的原因。

输出：

- AI_USAGE.md。

涉及技能：

- `ai-usage-recorder`

验收：

- 读者能看到 AI 的使用是可控、可追踪、可验证的。

## 4. 实施边界

在你审阅并确认本计划前，暂不执行以下动作：

- 不编写 README、SKILLS_SPEC、DESIGN、TEST_MATRIX、AI_USAGE 的正式内容。
- 不创建 examples 样例文件。
- 不做最终提交。
- 不假设存在真实遗留源码。

本阶段只完成：

- 创建工作目录。
- 创建可审阅的计划文档。

## 5. 建议执行顺序

如果本计划通过审阅，建议按以下顺序推进：

1. 完成 README.md 的主工作流。
2. 完成 SKILLS_SPEC.md，定义可复用技能节点。
3. 完成 DESIGN.md 的模块设计和伪代码。
4. 完成 TEST_MATRIX.md 和 examples 样例。
5. 完成 AI_USAGE.md，把关键提示词映射到对应技能。
6. 做一次整体一致性审查，检查题目要求是否全部覆盖。
7. 根据审查结果补齐遗漏。

## 6. subagent 增强的逐步实现计划

### 步骤 1：新增 `subagent-delegation-planner` 技能

目标：

- 封装“是否启用 subagent”的判断逻辑。
- 为每个候选节点生成 subagent 任务书。

技能应包含：

- 启用 subagent 的判断标准。
- 强委派、可选委派、禁止委派分类。
- subagent 任务输入包模板。
- subagent 输出工件格式。
- 主代理回收产物后的合并规则。

验收：

- 技能通过 `quick_validate.py`。
- 能单独回答“这个遗留脚本改造任务哪些节点适合交给 subagent”。

### 步骤 1.5：补充 agents 配置目录

目标：

- 为每个技能补充 `agents/openai.yaml`，配置中文展示名、简短说明和默认调用提示词。
- 在插件根目录新增 `agents/subagents.yaml`，沉淀 subagent 角色、允许动作、禁止动作和必须返回的工件。

验收：

- 每个技能目录下都有 `agents/openai.yaml`。
- 插件根目录下有 `agents/subagents.yaml`。
- subagent 配置只描述任务边界，不绕过主代理最终决策。

### 步骤 2：更新总控技能

目标：

- 在 `legacy-data-modernization-workflow` 中加入 subagent 编排规则。
- 明确主代理是总控，不把最终决策交给 subagent。

需要补充：

- 何时先调用 `subagent-delegation-planner`。
- 哪些节点可并行委派。
- subagent 产物如何进入工作流门禁。
- 冲突结果如何由主代理收敛。

验收：

- 总控技能能说明“单代理模式”和“多代理模式”的差异。
- 总控技能仍然保持简洁，不把每个 subagent 节点的细节重复写一遍。

### 步骤 3：更新强委派节点

目标：

- 给强委派节点加入“作为 subagent 任务时的边界”。

涉及技能：

- `repo-entrypoint-mapper`
- `consumer-contract-mapper`
- `legacy-behavior-characterizer`
- `compatibility-reviewer`
- `gray-release-planner`

每个技能补充：

- 可委派原因。
- subagent 允许做什么。
- subagent 禁止做什么。
- 必须返回的工件。

验收：

- 每个强委派节点都能作为独立子任务运行。
- 输出能被主代理直接合并到工作流中。

### 步骤 4：更新受控委派节点

目标：

- 给受控委派节点加入“建议可由 subagent 草拟，但主代理必须统一规则”的说明。

涉及技能：

- `dirty-data-case-designer`
- `refactor-slicer`

验收：

- 两个技能都明确主代理需要复核业务规则和切片顺序。
- subagent 不会直接决定最终行为变更。

### 步骤 5：更新 WORKFLOW.md

目标：

- 将 subagent 策略写入用户可读的工作流文档。

需要补充：

- 单代理模式。
- 多代理模式。
- subagent 节点链路。
- 并行与串行边界。
- 主代理汇总规则。

验收：

- 使用方能看懂什么时候该启用 subagent。
- 使用方能看懂 subagent 产物如何进入完整流程。

### 步骤 6：更新 README.md 和 PUBLISHING.md

目标：

- README 中补充插件支持 subagent 协作。
- PUBLISHING 中补充变更后重新校验和重新推送步骤。

验收：

- 仓库首页能说明这是“可单代理使用，也可多代理协作”的插件。
- 发布说明能指导后续版本更新。

### 步骤 7：验证、提交、推送

目标：

- 确保新增技能和修改后的插件仍可安装。

需要执行：

- 所有技能运行 `quick_validate.py`。
- 插件整体运行 `validate_plugin.py`。
- 检查常见英文说明残留。
- 提交并推送到 `git@github.com:taroandcandy/10jqka-project.git`。

验收：

- 所有校验通过。
- GitHub 仓库包含新增 subagent 策略和技能。

## 7. 题目六阶段补全计划

### 步骤 A：新增 `feature-extension-planner`

目标：

- 将“扩展”阶段从 `dirty-data-case-designer` 和 `refactor-slicer` 中独立出来。
- 专门描述如何支持批量处理多只股票、按行业输出、停牌缺失数据处理，同时保持旧输出兼容。

技能应包含：

- 输入：入口地图、消费者契约、旧行为基线、新需求、脏数据规则。
- 执行步骤：区分旧路径和新路径，设计多股票分组、行业聚合、新输出路径、诊断输出和兼容适配层。
- 输出格式：扩展设计表、旧行为保持项、新行为新增项、风险和验收标准。
- subagent 策略：可作为受控委派节点，由 subagent 草拟扩展方案，主代理统一业务规则和兼容边界。

验收：

- 扩展阶段成为显式节点。
- 能直接回答题目中新需求如何落地，而不只停留在重构层面。

### 步骤 B：新增 `regression-validation-planner`

目标：

- 将“验证”阶段从兼容性审查中独立出来。
- 专门设计完整测试矩阵和验证策略。

技能应包含：

- 单元测试。
- 基准文件回归测试。
- 结构契约测试。
- 下游报表冒烟测试。
- 新旧结果差异对比。
- 脏数据诊断验证。
- 批量处理性能验证。

subagent 策略：

- 适合强委派给 subagent，因为测试矩阵和验证项容易展开，且可以独立验收。

验收：

- 验证阶段成为显式节点。
- 能覆盖题目要求的测试矩阵、预期结果、下游兼容性验证。

### 步骤 C：新增 `ai-usage-recorder`

目标：

- 专门对应题目要求中的 AI_USAGE.md。
- 记录 AI 在理解、重构、测试和审查中的关键上下文、输出和验证方式。

技能应包含：

- 至少 3 条关键提示词模板，建议覆盖理解、行为刻画、扩展设计、验证、审查。
- 每条提示词的上下文、约束、输出格式和验收条件。
- AI 输出如何被人工验证。
- 一次拒绝 AI 大范围重写建议的原因。

subagent 策略：

- 可选委派给 subagent 草拟记录，但最终口径由主代理确认。

验收：

- AI_USAGE.md 不再只是计划中的交付物，而是有独立技能负责生成。

### 步骤 D：更新总控技能链路

目标：

- 将新增三个技能加入 `legacy-data-modernization-workflow` 默认链路。
- 让总控技能显式覆盖“理解 -> 行为刻画 -> 扩展 -> 重构 -> 验证 -> 灰度交付”。

验收：

- 总控技能中不再把扩展和验证隐含在其他节点里。
- 每个题目阶段都能映射到至少一个显式技能。

### 步骤 E：更新 subagent 配置

目标：

- 在 `agents/subagents.yaml` 中加入新增技能对应的角色。
- 明确 `feature-extension-planner` 和 `ai-usage-recorder` 为受控或可选委派，`regression-validation-planner` 为强委派。

验收：

- subagent 角色配置覆盖新增节点。
- 主代理仍保留最终规则统一和交付口径。

### 步骤 F：更新 README、WORKFLOW、PUBLISHING

目标：

- README 中更新题目要求对应关系。
- WORKFLOW 中更新节点链路。
- PUBLISHING 中更新当前版本说明和校验范围。

验收：

- 出题人可以一眼看出六阶段全部显式覆盖。
- 文档和插件文件结构一致。

### 步骤 G：全量验证、提交、推送

目标：

- 保证新增节点后插件仍然有效。

需要执行：

- 运行所有技能 `quick_validate.py`。
- 运行插件 `validate_plugin.py`。
- 扫描术语，确保继续统一使用 `subagent`。
- 提交本次补全。

验收：

- 所有校验通过。
- Git 历史中有清晰提交说明。

## 8. 审阅关注点

请重点确认：

- 是否认可“工作流搭建”作为答案主线。
- 是否认可“工作流编排层 + 单点技能节点”作为更精确的答案结构。
- 是否需要真的实现 Python 代码，还是只提交方案和伪代码。
- 是否需要把答案写得更像工程交付文档，还是更像笔试论述题。
- 是否保留 examples 目录作为加分材料。
- 是否认可新增 `subagent-delegation-planner` 作为 subagent 委派策略节点。
- 是否希望 subagent 策略单独成文档，还是合并到 WORKFLOW.md。
- 是否认可新增 `feature-extension-planner`、`regression-validation-planner`、`ai-usage-recorder` 来补齐题目六阶段。
