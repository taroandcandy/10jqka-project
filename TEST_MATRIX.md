# 测试矩阵、预期结果与灰度回滚

## 样例文件

| 文件 | 用途 |
| --- | --- |
| `examples/legacy_single_stock.csv` | 单股票旧路径基准输入 |
| `examples/multi_stock_with_industry.csv` | 多股票、跨行业、停牌、重复行、空字段样例 |
| `examples/expected_legacy_output.csv` | 旧输出格式预期结果 |
| `examples/expected_industry_output.csv` | 行业聚合预期结果 |

## 处理规则

- 移动平均线示例使用 `ma3`，便于人工验算；真实脚本可配置 `ma5`、`ma10` 等窗口。
- 同一股票同一交易日重复行，保留最后一条，并在诊断输出中记录 `duplicate_row`。
- 移动平均按连续交易日窗口计算；窗口内存在停牌缺失或空收盘价时，该日 MA 为空。
- 收盘价为空的记录不参与行业均值，状态记为 `invalid`。
- 停牌缺失交易日不伪造价格，状态记为 `suspended` 或 `missing`，不参与行业均值。
- 旧输出只保留旧报表依赖字段：`date,close,ma3`。
- 行业输出为新增文件，不覆盖旧输出。

## 自建测试矩阵

| 编号 | 场景 | 输入 | 预期结果 | 风险 | 验证方法 |
| --- | --- | --- | --- | --- | --- |
| T01 | 单股票正常 MA | `legacy_single_stock.csv` | 第 3 个交易日开始产生 `ma3`，旧输出字段为 `date,close,ma3` | 计算窗口或排序错误 | 对照 `expected_legacy_output.csv` |
| T02 | 多股票混合输入 | `multi_stock_with_industry.csv` | 按 `stock_code` 分组计算，互不串窗 | 股票之间数据串算 | 检查每只股票的 MA 独立计算 |
| T03 | 跨行业聚合 | `multi_stock_with_industry.csv` | 按 `trade_date + industry` 聚合，输出 `stock_count` 和 `valid_stock_count` | 行业维度遗漏或聚合口径不明 | 对照 `expected_industry_output.csv` |
| T04 | 停牌缺失交易日 | 多股票样例中 `000001` 缺少 `2026-01-03` | 不补收盘价，不参与 MA 和行业均值 | 伪造行情导致指标失真 | 检查诊断输出和行业有效股票数 |
| T05 | 重复行 | 多股票样例中 `600000` 的 `2026-01-02` 重复 | 保留最后一条 `close=8.20` | 重复记录导致 MA 偏高或偏低 | 检查去重后股票级序列 |
| T06 | 空字段 | 多股票样例中 `000001` 的 `2026-01-04` close 为空 | 状态为 `invalid`，不参与均值 | 空值被当作 0 | 检查 MA 和行业均值没有使用空值 |
| T07 | 旧输出兼容 | 单股票默认运行 | 文件名、字段、顺序、日期格式、精度保持旧契约 | 下游报表无法读取 | 结构契约测试和下游报表冒烟 |
| T08 | 新旧双写 | 开启新输出和旧输出 | 旧输出仍产生，新行业输出单独产生 | 新功能覆盖旧文件 | 检查输出路径和文件 hash |
| T09 | 性能验证 | 放大到 1000 只股票、252 个交易日 | 批处理耗时在旧脚本单股票线性外推可接受范围内 | 批量处理超时影响每日任务 | 记录耗时、内存、失败股票数 |
| T10 | 异常诊断 | 输入缺列、非法日期、非法数字 | 生成诊断报告，主输出不静默吞错 | 数据质量问题无法追踪 | 检查诊断文件和退出码 |

## 预期输出说明

### 旧输出

旧输出只面向存量报表，保持旧字段：

```text
date,close,ma3
2026-01-01,10.00,
2026-01-02,11.00,
2026-01-03,12.00,11.00
2026-01-04,13.00,12.00
2026-01-05,14.00,13.00
```

### 行业输出

行业输出是新增文件，包含聚合维度和有效样本数：

```text
trade_date,industry,stock_count,valid_stock_count,avg_close,avg_ma3
2026-01-03,银行,2,1,8.30,8.17
2026-01-05,银行,2,1,8.40,8.30
```

## 下游报表兼容性验证

| 验证项 | 检查方式 | 通过标准 |
| --- | --- | --- |
| 文件路径 | 比对旧配置和新配置默认值 | 默认仍写入旧路径 |
| 字段集合 | 读取 CSV header | 旧输出字段名和顺序完全一致 |
| 日期格式 | 抽样检查日期字符串 | 仍为旧报表可解析格式 |
| 数值精度 | 比对小数位和四舍五入口径 | 与旧脚本一致 |
| 空值表示 | 检查窗口不足和非法数据输出 | 与旧输出空值约定一致 |
| 排序 | 检查日期升序和股票默认范围 | 旧报表不需要改查询逻辑 |
| 报表冒烟 | 用旧报表读取新旧两份输出 | 报表无字段缺失、解析错误或明显行数异常 |

## 灰度指标

| 指标 | 监控目的 | 告警阈值 |
| --- | --- | --- |
| 旧输出生成成功率 | 保证存量任务不中断 | 低于 100% 立即告警 |
| 旧输出结构差异数 | 检查字段兼容性 | 大于 0 阻断发布 |
| 新旧单股票结果差异率 | 检查计算回归 | 非预期差异大于 0 阻断发布 |
| 输入行数和输出行数比 | 发现异常丢数 | 偏离历史均值超过 5% |
| 重复行率 | 监控数据源质量 | 超过历史 P95 告警 |
| 空字段率 | 监控脏数据变化 | 超过历史 P95 告警 |
| 停牌和缺失标记数 | 检查交易日处理 | 异常突增需人工确认 |
| 下游报表成功率 | 验证消费链路 | 低于 100% 回滚 |
| 批处理耗时 | 保证每日调度窗口 | 超过调度窗口 80% 告警 |

## 回滚条件

出现以下任一情况，应立即回滚到旧脚本或关闭新功能开关：

- 旧输出文件缺失、字段变化或字段顺序变化。
- 下游报表读取失败。
- 新旧单股票结果出现无法解释的差异。
- 批处理耗时超过每日调度窗口。
- 行业聚合指标样本数异常，且无法在诊断报告中解释。
- 诊断输出显示输入数据大面积异常，继续发布会误导报表使用者。

## 灰度步骤

1. 影子运行：新脚本读取同样输入，只产出隔离目录下的新输出。
2. 双写验证：旧脚本继续服务报表，新脚本同时写旧兼容输出和新行业输出。
3. 小股票池灰度：选择 5 到 10 只流动性稳定股票验证多股票路径。
4. 行业灰度：选择一个行业开启行业聚合输出。
5. 扩大范围：逐步扩展到全量股票和全部行业。
6. 保留回滚窗口：至少保留一个完整交易周的旧脚本和旧输出。
